export const episodeCodeFormatter = (seasonNum: number, episodeNum: number): string => {
    
    
    return ""
}

let number = 20;
console.log(number.toLocaleString("en-UK", {minimumIntegerDigits: 2}))

/*
S02E07 would be the code for the 7th episode of the 2nd season. S2E7 would be incorrect.
*/